# RBAC system in React.js and Node.js

### Node Module Installation
```
# Go to `/server` directory and run:
$ yarn
# Go to `/client` directory and run;
$ yarn
```

### How to run
```
# Go to `/server` directory and run:
$ yarn start

# Go to `/client` directory and run:
$ yarn start
```